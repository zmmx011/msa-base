# This workflow will build a Java project with Gradle and cache/restore any dependencies to improve the workflow execution time
# For more information see: https://help.github.com/actions/language-and-framework-guides/building-and-testing-java-with-gradle

name: Api Gateway Action

on:
  push:
    paths:
      - ".github/workflows/api-gateway-action.yaml"
      - "api-gateway/**"
jobs:
  build:
    runs-on: self-hosted
    steps:
      - uses: actions/checkout@v2
      - name: Set up JDK 17
        uses: actions/setup-java@v2
        with:
          java-version: '17'
          distribution: 'adopt'
          cache: gradle
      - name: Login Docker
        if: github.event_name != 'pull_request'
        uses: actions/login-action@v1.10.0
        with:
          registry: kds.inveniacorp.com
          username: ${{ github.repository_owner }}
          password: ${{ secrets.TOKEN }}
      - name: Docker Compose Build
        run: docker-compose build apigateway
      - name: Docker Push
        run: docker push kds.inveniacorp.com/${{ github.repository }}/api-gateway:latest
